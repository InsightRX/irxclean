% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_influential_individuals.R
\name{plot_influential_dofv}
\alias{plot_influential_dofv}
\title{Plot dOFV trajectory from iterative influential individual removal}
\usage{
plot_influential_dofv(results, dofv_threshold = NULL, label_ids = TRUE)
}
\arguments{
\item{results}{A results list from \code{remove_influential_individuals()}.}

\item{dofv_threshold}{Numeric or \code{NULL}.  If supplied, a horizontal
reference line is drawn at this value (e.g. 3.84).  Default \code{NULL}.}

\item{label_ids}{Logical.  Annotate each point with the removed subject ID?
Default \code{TRUE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a waterfall/trajectory plot of the change in total OFV
(\code{dOFV = sumOFV_before - sumOFV_after}) for each subject removed
by \code{remove_influential_individuals()}.  An optional threshold line
marks the stopping criterion when \code{dofv_threshold} was used.
}
\examples{
\dontrun{
results <- remove_influential_individuals(
  dat = "my_data", mod = "my_model",
  run_id = "iir_run1", dofv_threshold = 3.84
)
plot_influential_dofv(results, dofv_threshold = 3.84)
}

}
\seealso{
\code{remove_influential_individuals()}
}
