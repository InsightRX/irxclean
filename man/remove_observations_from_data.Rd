% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_observations_from_data.R
\name{remove_observations_from_data}
\alias{remove_observations_from_data}
\title{Apply user-reviewed observation exclusions to produce a cleaned dataset}
\usage{
remove_observations_from_data(
  data,
  results,
  n = NULL,
  columns = list(ID = "ID", TIME = "TIME", DV = "DV")
)
}
\arguments{
\item{data}{A data frame containing the original NONMEM dataset.}

\item{results}{A results list from [remove_erroneous_obs()] or
[load_removal_results()].}

\item{n}{Integer. Number of observations to remove (taken from the first
`n` iterations in `results$rem`).  Default: all removed observations.}

\item{columns}{Named list specifying column labels for matching.
Default: `list(ID = "ID", TIME = "TIME", DV = "DV")`.}
}
\value{
The input `data` frame with the identified observations removed.
}
\description{
After running \code{remove_erroneous_obs()} and reviewing the flagged
observations, use this function to apply exclusions and produce a cleaned
dataset suitable for final analysis.  The top `n` flagged observations
(by iteration order) are excluded.
}
\section{User review}{

Flagged observations should be investigated before calling this function.
A flagged observation is not necessarily erroneous; it may represent a
genuine patient population subgroup or an unusual but valid measurement.
Final exclusion decisions rest with the analyst.

Matching is performed on a composite key of rounded (`3` decimal places)
`ID`, `TIME`, and `DV` values concatenated together, so that each flagged
observation is identified as a unit rather than by independent column
membership.  A warning is raised if the number of rows removed does not
equal `n`.
}

\examples{
\dontrun{
original_data <- read.csv("my_data.csv")
results       <- load_removal_results("err_rem1")
cleaned       <- remove_observations_from_data(original_data, results, n = 5)
}

}
